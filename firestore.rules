rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    match /proposals/{proposalId} {
      allow read, write: if true;
    }

    match /proposalManager/{documentId} {
      allow read, write: if true;
    }
  }
}
